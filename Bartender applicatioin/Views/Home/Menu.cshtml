@model List<CocktailMenu>

<h2>Cocktail Menu</h2>

@foreach (var cocktail in Model)
{
    <div>
        <h3>@cocktail.Name</h3>
        <p>@cocktail.Ingredients</p>
        <p>Price: $@cocktail.Price</p>
        <a asp-action="PlaceOrder" asp-route-id="@cocktail.Id">Place Order</a>
    </div>
}